import React from 'react';

const NavbarLink = ({navData}) => {
    console.log(navData)
    return (
        <div>
             {/* <li><NavLink to={"/"} 
            className={({isActive}) => `font-semibold mr-1 text-center ${isActive && ' bg-[#244d3fFF] text-white'}`}>Home</NavLink></li> */}
        </div>
    );
};

export default NavbarLink;